# Production Environment Variables for SAWS Frontend
# Copy this to .env.production and update with actual values before building

# The base URL for the backend API.
# Once the backend module is deployed to Cloud Run, put its URL here.
# Example: https://saws-backend-xxxxx-uc.a.run.app/api
VITE_API_BASE_URL=
